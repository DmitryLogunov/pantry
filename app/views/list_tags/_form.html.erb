<%= form_with(model: list_tag, local: true) do |form| %>
  <% if list_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(list_tag.errors.count, "error") %> prohibited this list_tag from being saved:</h2>

      <ul>
      <% list_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :list_tag_title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
